APP_NAME=Portfolio_Slashev
APP_ENV=production
APP_KEY=
APP_DEBUG=false
APP_TIMEZONE=UTC
APP_URL=https://portfoliolaravel-production-7f98.up.railway.app/

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

LOG_CHANNEL=stack
LOG_LEVEL=debug

# Подключение к Railway MySQL
DB_CONNECTION=mysql
DB_HOST=gondola.proxy.rlwy.net
DB_PORT=19527
DB_DATABASE=railway
DB_USERNAME=root
DB_PASSWORD=AtXeTSDUDlFgDuugGAHgVOMSPbrrcJXo

SESSION_DRIVER=file
SESSION_LIFETIME=120

CACHE_DRIVER=file
QUEUE_CONNECTION=sync
FILESYSTEM_DISK=local

MAIL_MAILER=smtp
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=slasev903@gmail.com
MAIL_PASSWORD=wntmlihxndleuool
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=slasev903@gmail.com
MAIL_FROM_NAME="${APP_NAME}"

# Laravel Vite переменные
VITE_APP_NAME="${APP_NAME}"

# Переменные Railway MySQL (используются Railway автоматически, можно оставить)
MYSQL_DATABASE=railway
MYSQL_ROOT_PASSWORD=AtXeTSDUDlFgDuugGAHgVOMSPbrrcJXo
MYSQLUSER=root
MYSQLHOST=${RAILWAY_PRIVATE_DOMAIN}
MYSQLPORT=3306
MYSQLPASSWORD=${MYSQL_ROOT_PASSWORD}
MYSQL_URL="mysql://${MYSQLUSER}:${MYSQL_ROOT_PASSWORD}@${MYSQLHOST}:${MYSQLPORT}/${MYSQL_DATABASE}"
MYSQL_PUBLIC_URL="mysql://${MYSQLUSER}:${MYSQL_ROOT_PASSWORD}@${RAILWAY_TCP_PROXY_DOMAIN}:${RAILWAY_TCP_PROXY_PORT}/${MYSQL_DATABASE}"
MYSQLDATABASE=${MYSQL_DATABASE}
